#include <stdio.h>
#include <ctype.h>

/**
 *_isupper - function that checks for uppercase character.
 *@n: integer
 *Return: 1 if c is uppercase 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	if (c >= 97 && c <= 122)
		return (0);
}
