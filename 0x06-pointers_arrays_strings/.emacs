( setq c-default-style " bsd "
       c-basic-offset 8
       tab-width 8
       indent-tabs-mode t )

( nécessite  'espace blanc' )
( setq whitespace-style '(face lignes vides-queue fin))
( global-whitespace-mode  t )

( setq numéro de colonne-mode t )
